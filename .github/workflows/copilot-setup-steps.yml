name: "Copilot Setup Steps"

# Automatically run the setup steps when they are changed to allow for easy validation, and
# allow manual testing through the repository's "Actions" tab
on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/copilot-setup-steps.yml
  pull_request:
    paths:
      - .github/workflows/copilot-setup-steps.yml

jobs:
  # The job MUST be called `copilot-setup-steps` or it will not be picked up by Copilot.
  copilot-setup-steps:
    runs-on: ubuntu-latest

    # Set the permissions to the lowest permissions possible needed for your steps.
    # Copilot will be given its own token for its operations.
    permissions:
      # If you want to clone the repository as part of your setup steps, for example to install dependencies, you'll need the `contents: read` permission. If you don't clone the repository in your setup steps, Copilot will do this for you automatically after the steps complete.
      contents: read

    # You can define any steps you want, and they will run before the agent starts.
    # If you do not check out your code, Copilot will do this for you.
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      # Example: Download and install custom fonts for testing
      - name: Download test fonts
        run: |
          mkdir -p custom-fonts
          curl -L "https://github.com/google/fonts/raw/main/ofl/notosanstc/NotoSansTC-Regular.otf" -o custom-fonts/NotoSansTC-Regular.otf
          curl -L "https://github.com/google/fonts/raw/main/ofl/notosanstc/NotoSansTC-Bold.otf" -o custom-fonts/NotoSansTC-Bold.otf

      - name: Install custom fonts
        uses: ./
        with:
          fonts-dir: 'custom-fonts'
        timeout-minutes: 3

      # - name: Set up Node.js
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: "20"
      #     # cache: "npm" requires package.json to exist
      #     # Error: Dependencies lock file is not found in /home/runner/work/install-custom-fonts-action/install-custom-fonts-action. Supported file patterns: package-lock.json,npm-shrinkwrap.json,yarn.lock
      #     # cache: "npm"

      # - name: Upgrade npm to latest
      #   run: npm install -g npm@latest

      # - name: Install JavaScript dependencies
      #   run: npm ci
